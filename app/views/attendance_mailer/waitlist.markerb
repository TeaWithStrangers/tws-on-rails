Hey hey <%= @user.name %>!

Just confirming that you have been added to the wait list for the following tea time:

<%= render partial: "shared_mailers/info_nohost", locals: {tea_time: @tea_time} %>

If/when a spot opens up, you'll get an email from me so you can join in! Usually, life comes up and one person has to drop out the day-of, so keep an eye out for an email then.

Cheers,  
<%= @tea_time.host.name %>
