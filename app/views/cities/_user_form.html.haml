= form_for @city, url: suggest_create_cities_path do |f|
  = f.label :name
  = f.text_field :name
  .city-confirmation-text
    %h2
      Double check your spelling! And make sure it’s just the city or school
      name.
    %p
      After you confirm, we’ll make sure everything checks out and email you
      when your friends and neighbors can join you for Tea With Strangers.
  - if @city.errors.any?

    #error_explanation
      %h2= "#{pluralize(@city.errors.count, "error")} prohibited this city from being saved:"
      %ul
        - @city.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Submit', data: { confirmation: false }, class: 'save-city'
