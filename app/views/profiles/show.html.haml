.container.dashboard
  = render partial: 'shared/nav_header'
  .row
    .wide
      %h2= "Welcome home, #{current_user.name}!"
      .third.left
        = render partial: 'sidebar'
      %hr
      .two-third.right
        = link_to "View past tea times", history_path
        = render partial: 'host_tea_time_info' if current_user.host?
        = render partial: 'user_tea_time_info'
