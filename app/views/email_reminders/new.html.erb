<%= render partial: 'shared/header' %>
<%= render partial: 'profiles/navbar' %>
<div class="container dashboard">
  <div class="dash-intro">
    <div class="photo-container">
      <div class="host-photo">
        <img class="host-img" src="<%= @current_user.avatar.url(:landscape) %>"/>
      </div>
    </div>
    <div class="hello">
      <h2>
        Welcome home, <%= "#{current_user.nickname}!" %>
      </h2>
      <h3 class="gray">
        You look real good today.
      </h3>
    </div>
  </div>
  <div class="dash-body">
    <div class="edit-profile form">
      <h2>
        What do you want your reminder email to say?
      </h2>

      <p>
        Attendees get reminder emails 48hrs and 12hrs before tea time starts. These are from "The Robots at Tea With Strangers". You can customize the T-48hrs note to be from "<%= current_user.nickname %> at Tea With Strangers" to include any specific information and allow attendees to reply directly to you.
      </p>

      <div class="email-reminder form">
      <%= render 'form' %>
    </div>
  </div>
</div>
