.row.tea-time
  .two.column
    %img.host-avatar.tiny{src: tea_time.host.avatar.url(:thumb)}
  .seven.columns
    %p.time-and-location
      = tea_time.friendly_time
      %br
      = tea_time.location
  .four.columns.host-name
    %p= tea_time.host.name
  = button_to attendance_tea_time_path(tea_time), class: ["button", tea_time.spots_remaining? ? :green : :red] do
    .three.columns
      - if tea_time.spots_remaining?
        = "#{tea_time.spots_remaining} spots left"
      - else
        Full
